<Window x:Class="CarRentalManagment.Views.AddVehicleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:CarRentalManagment.Utilities.Converters"
        mc:Ignorable="d"
        Title="Add Vehicle"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Background="#f5f5f9"
        d:DesignWidth="500"
        d:DesignHeight="600">
    <Window.Resources>
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Padding="24">
        <StackPanel Spacing="16">
            <TextBlock Text="Add a new vehicle"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#262645" />

            <Grid ColumnSpacing="16">
                <Grid.Resources>
                    <Style x:Key="InputTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource RoundedTextBoxStyle}">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="10" />
                    </Style>
                    <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="10" />
                        <Setter Property="Background" Value="#f2f4ff" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="Foreground" Value="#333" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                    </Style>
                </Grid.Resources>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="12">
                    <TextBlock Text="Vehicle Information" FontWeight="SemiBold" Foreground="#4f4f6d" />
                    <StackPanel>
                        <TextBlock Text="Make" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Make, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Model Year" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.ModelYear, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="VIN" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Vin, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Category" Foreground="#6b6b8a" />
                        <ComboBox Style="{StaticResource InputComboBoxStyle}"
                                  ItemsSource="{Binding Categories}"
                                  SelectedItem="{Binding Vehicle.Category}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Fuel" Foreground="#6b6b8a" />
                        <ComboBox Style="{StaticResource InputComboBoxStyle}"
                                  ItemsSource="{Binding FuelTypes}"
                                  SelectedItem="{Binding Vehicle.Fuel}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Seats" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Seats, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Color" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Color, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="12" Margin="0,24,0,0">
                    <StackPanel>
                        <TextBlock Text="Model" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Model, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Plate Number" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.PlateNumber, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Daily Rate" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.DailyRate, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Transmission" Foreground="#6b6b8a" />
                        <ComboBox Style="{StaticResource InputComboBoxStyle}"
                                  ItemsSource="{Binding Transmissions}"
                                  SelectedItem="{Binding Vehicle.Transmission}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Status" Foreground="#6b6b8a" />
                        <ComboBox Style="{StaticResource InputComboBoxStyle}"
                                  ItemsSource="{Binding Statuses}"
                                  SelectedItem="{Binding Vehicle.Status}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Doors" Foreground="#6b6b8a" />
                        <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding Vehicle.Doors, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </Grid>

            <StackPanel>
                <TextBlock Text="Description" Foreground="#6b6b8a" />
                <TextBox Style="{StaticResource RoundedTextBoxStyle}"
                         Text="{Binding Vehicle.Description, UpdateSourceTrigger=PropertyChanged}"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True" />
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#d64545"
                       TextWrapping="Wrap"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Save Vehicle" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
