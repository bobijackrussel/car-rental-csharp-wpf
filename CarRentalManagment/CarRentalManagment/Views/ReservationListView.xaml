<UserControl x:Class="CarRentalManagment.Views.ReservationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:CarRentalManagment.Utilities.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <DropShadowEffect x:Key="CardDropShadow" BlurRadius="16" ShadowDepth="4" Opacity="0.15" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ComboBox ItemsSource="{Binding Filters}"
                          SelectedItem="{Binding SelectedFilter}"
                          DisplayMemberPath="DisplayName"
                          Width="160"
                          Padding="8"
                          Background="#f2f4ff"
                          BorderThickness="0" />
                <Button Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Margin="12,0,0,0"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Width="140" Height="4" IsIndeterminate="True" />
                <TextBlock Text="Loading reservations..." Margin="8,0,0,0" Foreground="#6b6b8a" />
            </StackPanel>
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="#d64545"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"
                   Margin="0,0,0,12" />

        <Border Grid.Row="2" Background="#f5f5f9" CornerRadius="12">
            <Grid>
                <ListView ItemsSource="{Binding Reservations}"
                          Margin="0"
                          Background="Transparent"
                          BorderThickness="0"
                          IsSynchronizedWithCurrentItem="True">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="12" Padding="18" Effect="{StaticResource CardDropShadow}">
                                <Border.Resources>
                                    <Style TargetType="Border" x:Key="StatusBadgeStyle">
                                        <Setter Property="Padding" Value="8,2" />
                                        <Setter Property="CornerRadius" Value="10" />
                                        <Setter Property="Background" Value="#dfe6ff" />
                                        <Setter Property="BorderBrush" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                <Setter Property="Background" Value="#fff4d6" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Confirmed">
                                                <Setter Property="Background" Value="#d5f5e5" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                <Setter Property="Background" Value="#fde4e4" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                <Setter Property="Background" Value="#e1e6ff" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Resources>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Spacing="6">
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <TextBlock Text="{Binding VehicleDisplayName}"
                                                       FontWeight="SemiBold"
                                                       FontSize="16"
                                                       Foreground="#262645" />
                                            <Border Style="{StaticResource StatusBadgeStyle}">
                                                <TextBlock Text="{Binding StatusDisplay}" Foreground="#3c3c54" />
                                            </Border>
                                        </StackPanel>

                                        <TextBlock Text="{Binding DateRangeDisplay}" Foreground="#6b6b8a" />
                                        <TextBlock Text="{Binding TotalAmountDisplay, StringFormat=Total\: {0}}" Foreground="#262645" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Notes}" Foreground="#6b6b8a" TextWrapping="Wrap"
                                                   Visibility="{Binding Notes, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />

                                        <StackPanel Orientation="Horizontal" Spacing="8"
                                                    Visibility="{Binding CancellationReason, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
                                            <TextBlock Text="Cancellation reason:" Foreground="#d64545" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding CancellationReason}" Foreground="#d64545" />
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Spacing="8">
                                        <Button Content="Cancel Reservation"
                                                Command="{Binding DataContext.CancelReservationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Visibility="{Binding CanCancel, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <TextBlock Text="Cancelling..."
                                                   Foreground="#6b6b8a"
                                                   Visibility="{Binding IsCancelling, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Border Background="#ffffff" CornerRadius="12" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" Spacing="6">
                        <TextBlock Text="No reservations to show yet."
                                   Foreground="#6b6b8a" />
                        <TextBlock Text="Create a reservation to see it listed here."
                                   Foreground="#9a9ab5" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
