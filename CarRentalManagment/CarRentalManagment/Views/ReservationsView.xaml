<UserControl x:Class="CarRentalManagment.Views.ReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CarRentalManagment.Views"
             xmlns:converters="clr-namespace:CarRentalManagment.Utilities.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Background="#f5f5f9">
        <Border Background="White" CornerRadius="20" Margin="32" Padding="32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding Title}"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Foreground="#262645" />
                        <TextBlock Text="{Binding Description}"
                                   FontSize="14"
                                   Foreground="#5a5a7b"
                                   TextWrapping="Wrap"
                                   Margin="0,6,0,0" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="New Reservation"
                            Command="{Binding CreateReservationCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="24,0,0,0"
                            VerticalAlignment="Center"
                            Visibility="{Binding CanCreateReservation, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Border Grid.Column="2"
                            VerticalAlignment="Center"
                            Background="#fdeaea"
                            CornerRadius="12"
                            Padding="12"
                            Margin="16,0,0,0"
                            Visibility="{Binding CanCreateReservation, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <TextBlock Text="Sign in to create reservations"
                                   Foreground="#c24a4a" />
                    </Border>
                </Grid>

                <Separator Grid.Row="1" Margin="0,24,0,24" />

                <local:ReservationListView Grid.Row="2" DataContext="{Binding ReservationList}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
